Para ejecutarlo primero instala las siguientes librerias

instalar sympy de la siguiente forma:
pip install sympy

intalar la biblioteca tkinter de la siguiente forma:
pip install tk

para tener la ultima version ejecuta:
git pull origin main
